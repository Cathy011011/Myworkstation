# 字段字典 · 六张数据表

> 对应 `05-脚本/06-tables-schema.json`，是建表时唯一依据。
> **迁移禁忌**：字段一旦写入数据就**不允许改类型**（改类型会清空存量单元格），只允许增字段。因此下表必须一次成型。

## 跨表统一约定

| 约定 | 做法 | 原因 |
|---|---|---|
| 日期 | 双写：`<名>日期键` **text** `yyyy-mm-dd` 负责一切过滤排序；`<名>日期` **date** 仅作表格展示 | date 回读是 ISO8601 带 Z，东八区存在差一天的风险；改用纯字符串比较可 100% 规避 |
| 时间 | 一律 **text** `HH:mm`，零填充 | "09:30 开会"是墙上时间，不该随设备时区漂移 |
| 周期查询 | 冗余 `<名>周期键` **text**，如 `2026-W37` / `2026-09` | 一次 `equals` 命中整周整月，无需全量拉取后在客户端过滤 |
| 标签 | multi_select 固定词表 + 「自定义标签」text 兜底 | SDK 无法新建选项，写入不存在的选项会报错 |
| 空值 | 渲染前一律 `row[x] \|\| ''` | 查询返回的空值是 `null` |
| 排序/筛选字段 | 必须逐字等于 `getSchema()` 返回的字段名 | 字段名不匹配会直接查询失败 |

---

## 表 1 · 日程表

| 字段 | 类型 | 说明 |
|---|---|---|
| 标题 | text | 日程名称 |
| 日期键 | text | `yyyy-mm-dd`，**主过滤字段** |
| 日期 | date | 展示用 |
| 开始时间 | text | `HH:mm` |
| 结束时间 | text | `HH:mm` |
| 类型 | select | 工作 / 学习 / 生活 / 健康 / 社交 / 家庭 / 其他 |
| 地点 | text | 选填 |
| 状态 | select | 待开始 / 进行中 / 已完成 / 已取消 |
| 提醒 | checkbox | 是否需要提醒 |
| 备注 | text | |
| 创建时间 | date | |

主视图：按 `日期键` 分组，`开始时间` 升序。

## 表 2 · 待办表

| 字段 | 类型 | 说明 |
|---|---|---|
| 标题 | text | |
| 优先级 | select | 重要紧急 / 重要 / 一般 / 低（**建表顺序即排序顺序**） |
| 状态 | select | 待办 / 进行中 / 已完成 / 已搁置 |
| 完成 | checkbox | 勾选完成 |
| 截止日期键 | text | 主过滤字段 |
| 截止日期 | date | 展示用 |
| 领域 | multi_select | 工作 / 学习 / 生活 / 健康 / 财务 / 家庭 |
| 计划周 | text | `2026-W37`，串联日程规划 |
| 完成时间 | date | |
| 备注 | text | |

## 表 3 · 翻译表

| 字段 | 类型 | 说明 |
|---|---|---|
| 原文 | text | |
| 译文 | text | AI 回填 |
| 源语言 | select | 中文 / 英文 / 日文 / 韩文 / 法文 / 德文 / 其他 |
| 目标语言 | select | 同上 |
| 模型 | select | DeepSeek / 通义千问 / Kimi / 智谱 / OpenAI / 自定义 |
| **状态** | select | 草稿 / 待处理 / 处理中 / 已完成 / 失败 |
| **处理方式** | select | 页面直连 / 代理回写 / 手动 |
| 错误信息 | text | 调用失败原因 |
| 处理时间 | date | |
| 收藏 | checkbox | |
| 标签 | multi_select | 常用词表 |
| 自定义标签 | text | 兜底 |
| 创建时间 | date | |

> 状态 + 处理方式两个字段是 AI 方案可切换的关键：直连失败时自动转为「代理回写」，由 WorkBuddy 侧处理后回写，**无需改表结构**。

## 表 4 · 问答表

| 字段 | 类型 | 说明 |
|---|---|---|
| 问题 | text | |
| 回答 | text | AI 回填 |
| 模型 | select | 同翻译表 |
| 状态 | select | 待处理 / 处理中 / 已完成 / 失败 |
| 处理方式 | select | 页面直连 / 代理回写 / 手动 |
| 错误信息 | text | |
| 处理时间 | date | |
| 已归档 | checkbox | |
| 标签 | multi_select | |
| 自定义标签 | text | |
| 评分 | number | 1–5 |
| 创建时间 | date | |

## 表 5 · 日程规划表

| 字段 | 类型 | 说明 |
|---|---|---|
| 规划标题 | text | |
| 周期类型 | select | 本周 / 下周 / 本月 / 本季度 / 年度 |
| 周期键 | text | `2026-W37` 或 `2026-09`，主过滤字段 |
| 开始日期键 | text | |
| 结束日期键 | text | |
| 开始日期 | date | 展示用 |
| 结束日期 | date | 展示用 |
| 进度 | number | 0–100 |
| 状态 | select | 未开始 / 进行中 / 已完成 / 已延期 |
| 关键结果 | text | 做到什么算完成 |
| 领域 | multi_select | 工作 / 学习 / 生活 / 健康 / 家庭 |
| 备注 | text | |

## 表 6 · 信息整理表

| 字段 | 类型 | 说明 |
|---|---|---|
| 标题 | text | 给这条摘录起的名字 |
| 来源 | url | `{ text, link }` |
| 来源名称 | text | 书名 / 播客 / 文章名 |
| 原文摘录 | text | 别人的原话 |
| 我的笔记 | text | **自己的整理**，与摘录分开存 |
| 标签 | multi_select | 方法论 / AI / 读书 / 健康 / 理财 / 职场 / 设计 / 其他 |
| 自定义标签 | text | 兜底 |
| 已归档 | checkbox | |
| 评分 | number | 1–5 |
| 创建时间 | date | |

---

## 建表 JSON（`create_database.py --schema`）

```json
{
  "title": "日程表",
  "properties": [
    { "name": "标题",     "config": { "text": "" } },
    { "name": "日期键",   "config": { "text": "" } },
    { "name": "日期",     "config": { "date": "2026-09-07T00:00:00Z" } },
    { "name": "开始时间", "config": { "text": "" } },
    { "name": "结束时间", "config": { "text": "" } },
    { "name": "类型",     "config": { "select": { "options": [{ "text": "工作" }, { "text": "学习" }, { "text": "生活" }, { "text": "健康" }, { "text": "社交" }, { "text": "家庭" }, { "text": "其他" }] } } },
    { "name": "状态",     "config": { "select": { "options": [{ "text": "待开始" }, { "text": "进行中" }, { "text": "已完成" }, { "text": "已取消" }] } } },
    { "name": "地点",     "config": { "text": "" } },
    { "name": "提醒",     "config": { "checkbox": false } },
    { "name": "备注",     "config": { "text": "" } },
    { "name": "创建时间", "config": { "date": "2026-09-07T00:00:00Z" } }
  ]
}
```

其余五表同构，完整六合一份的 JSON 见 `05-脚本/06-tables-schema.json`。

> 注：`date` 的 config 形态以实测为准，首张表先试建验证成功后，其余五张再批量执行。

## 运行时必知

- `db.query` 官方示例 `pageSize: 50`，**不翻页只能看到前 50 条** → 统一封装 `queryAll`（while `hasMore`，40 页熔断）
- select 查询只返回**选项文本**，filter 也只能用文本匹配
- filter 支持 `and` / `or` 树，日期区间用 `and[after T0, before T1]`
- date 写入统一补 `T00:00:00Z`
- image 字段在本项目暂不使用；如后续要加封面，走 image 字段而非手写 `<img src>`（会被 SDK 覆盖）
