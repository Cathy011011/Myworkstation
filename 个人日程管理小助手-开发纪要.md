# 个人日程管理小助手 · 开发纪要

> 项目：个人日程管理小助手（原名"生活工作台 V2"）
> 当前版本：**v1.1**（v1.0 基础上新增智能体问答）
> 线上地址：https://workbuddy.link/p/OxKRm0lpVdKq8P3qT30q74
> 记录时间：2026-09-13

---

## 一、产品概况

| 项 | 内容 |
|---|---|
| 产品名 | 个人日程管理小助手 |
| 形态 | 单文件 HTML 网页应用 |
| 数据存储 | WorkBuddy 资料库云端 6 张数据表 |
| 核心能力 | 多设备 / 多浏览器访问同一份数据，双向同步 |
| 功能模块 | 今日总览、日程、待办、翻译、问答、日程规划、信息整理 |
| 定稿版本 | v1.0 |

**在线使用**：任何设备浏览器打开链接即用。数据存在云端数据表，换设备打开数据都在；全新设备首次打开自动同步云端已有数据。

---

## 二、六张云端数据表

| 模块 | 数据表 ID |
|---|---|
| 日程 | `68N86PsnmbQqLsN9T3KnTP` |
| 待办 | `SW82b5oYxenJt2yxoFDdYK` |
| 翻译 | `xHK4R1lsTvn7yooUhMYPGa` |
| 问答 | `QDCvUTxoBMSsECyAvolP4s` |
| 日程规划 | `STVY3ZPJk79CpGThiDRDLS` |
| 信息整理 | `xPAXPE6kaWt7Ke4HEOnDyY` |

页面节点 ID：`OxKRm0lpVdKq8P3qT30q74`

---

## 三、版本演进（v11 → v1.0）

| 版本 | 主要内容 |
|---|---|
| v11 | 删除防假成功（墓碑等云端确认后才解除）+ 墓碑自动重试 + 拉取后自动补推未同步模块 |
| v12 | 顶栏增加版本号角标（用于验证浏览器缓存是否更新） |
| v13 | **修复"删除后复活"根因**：墓碑不再因云端"暂时查不到"而解除，改以云端明确确认为准；增加内容指纹识别错位记录 |
| v14 | 修复"输入到一半被清空"（重绘时保留表单内容）+ 墓碑 4 小时自动恢复显示 |
| v16 | 删除确认对话框 + 新设备首开采纳云端 + 日期对象解包 + 布尔判断统一；六模块 18/18 测试通过 |
| v1.0 | 更名定稿，清理全部历史版本与过程文档，文档瘦身 |
| **v1.1** | **新增智能体问答**（13 工具 + 对话循环 + 删除硬拦截 + 撤销）；顺带修掉 3 个历史潜在 bug |

### v1.1 变更明细

| 项 | 内容 |
|---|---|
| 新增功能 | 问答页「快速 / 智能体」双模式；智能体可调用 13 个工具读写真实数据 |
| 工具集 | 只读 7 个（查日程/列待办/列规划/检索问答/检索信息/全局搜索/统计概览）；写入 6 个（加日程/加待办/完成待办/改日程/加信息/删除） |
| 安全设计 | ① 删除**硬拦截**：智能体无权直接删，只能入「待确认队列」由用户点确认 ② 每轮开始重置撤销快照，撤销只回滚本轮 |
| 协议兼容 | 同时支持标准 `tool_calls` 与文本内 JSON 两种模型协议；HTTP 400/422/404 自动回落文本协议重试 |
| 会话持久化 | 存本机 localStorage（最近 40 条），刷新不丢，不跨设备同步 |
| 兼容性保证 | 快速问答 DOM **逐字保留 v1.0 结构**，默认模式仍是快速；六模块手动增删回归全通过 |
| 顺带修 bug | ① `snapshot` 缺模块键导致写操作崩溃 ② `schedulePush` 遇未知模块键 TypeError 中断整轮推送 ③ 时间越界（如 `25:00`）被静默裁剪，改为交给调用方走默认值 |
| 版本号 | `<title>` / 顶栏角标 / 侧栏标签三处 v1.0 → v1.1 |

### v1.1 测试结果

| 层次 | 用例数 | 结果 |
|---|---|---|
| 单元测试（`vm` 沙箱 + 假 DOM） | 44 | 44 通过 / 0 失败 |
| 浏览器集成测试（自包含 headless Chrome + Mock LLM） | 73 | 73 通过 / 0 失败 |
| UI 巡检（真实浏览器无 mock） | 19 | 19 通过 / 0 失败 |

其中集成测试含 **12 条安全红线断言**：智能体无法直接删除、删除转确认队列、确认后正确写墓碑、撤销完整回滚、XSS 内容转义、v16「导航点击失效」老 bug 复验。

---

## 四、关键缺陷与修复（核心技术价值）

### 1. 删除后刷新又复活

- **现象**：删除任务 → 点"刷新云端数据" → 被删的记录又回来了
- **根因**：墓碑裁决逻辑写成"云端按 id 查不到 = 已删除"，但云端在删除后存在**短暂查不到该记录的时间窗口**，墓碑被提前解除；等记录重新出现在查询结果里就复活了
- **修复**：墓碑只在云端**明确返回"已删除"**时才解除；查不到则保留隐藏（4 小时未确认才自动恢复显示，避免数据"永久消失"的恐慌）
- **验证**：同一用例分别跑旧版与新版对照 —— 旧版复活、新版不复活

### 2. 输入到一半内容被清空

- **现象**：正在填标题，过几秒内容突然没了
- **根因**：页面后台每次同步完成都会整体重绘 DOM，把正在输入的表单一起清掉
- **修复**：重绘前保存所有输入值、重绘后还原；数据没变化时跳过整页重绘

### 3. 新设备打开看不到自己的数据

- **现象**：换新浏览器打开，永远显示示例数据，云端真实数据不出现
- **根因**：首开时本地"示例数据"被判定为"有未同步修改"，同步策略优先保留本地，导致云端数据从未被采纳
- **修复**：新增"首开标记"，全新设备打开时若云端有数据，示例数据让位、直接采纳云端

### 4. 日期字段读回损坏

- **现象**：云端记录读回本地后日期字段变成 `[object Ob...]`，导致后续内容比对全部失配
- **根因**：日期字段读回是对象形态（`{date:...}`），代码未解包就直接转字符串
- **修复**：统一解包对象形态

### 5. "已删除"过滤从未生效

- **现象**：云端已标记删除的记录仍会显示出来
- **根因**：平台读回的布尔标记是对象形态（`{checkbox:true}`），而页面用严格等于 `=== true` 判断，永远不成立
- **修复**：统一布尔转换函数处理对象/字符串/布尔三种形态

### 6. 模块导航点击无响应（真实浏览器实测发现）

- **现象**：7 个模块导航中只有 2 个能点，其余点击无反应
- **根因**：导航按钮的 HTML 用**单引号字符串拼接**生成，`data-view="'+v.id+'"` 不会被 JS 插值，浏览器实际渲染成字面字符串
- **修复**：改为模板字符串正确插值
- **重要**：此缺陷**所有模拟测试都通过了**，只有真实浏览器 + 登录态实测才暴露（详见第六节教训）

### 7. 云端脏数据清理（服务端）

- 删除 5 条待办 + 3 条信息 + 3 条翻译 + 3 条问答 + 1 条规划的**空壳记录**（标题为空的垃圾数据）
- 日程表**重复记录去重**：同一任务因反复删除复活堆积了 5 份，清理为 1 份

---

## 五、同步与数据安全机制（定稿设计）

- **写操作**：本地即时生效 → 后台 diff 推送到云端（增/改/删逐条同步）
- **读操作**：点「刷新云端数据」或切回页面时拉取云端最新
- **状态徽章**：●已同步 / ◐同步中 / ○异常（可点重试）/ ○离线
- **删除机制**：软删除 + 墓碑保护 —— 删除立即生效、刷新不复活、云端确认后彻底清除
- **数据保护**：云端为空时绝不覆盖本地；本地有未推送修改时保留本地；全新设备首开采纳云端
- **AI 翻译/问答**：右上角「AI 设置」配置接口地址/模型/密钥，密钥只存本机浏览器，不进页面代码、不进云端表

**重要结论**：实测确认「刷新云端数据」按钮**不会删除用户数据** —— 它的逻辑是"先推本地未同步变更，再拉取云端"，删除只走软删除标记 + 墓碑保护。

---

## 六、测试体系与教训

### 测试层次

| 层次 | 覆盖内容 | 结果 |
|---|---|---|
| 模拟数据测试 | 六模块 ×（输入保护 / 新增防丢 / 删除防复活） | 18/18 通过 |
| 新旧版本对照 | 同一用例跑旧版复现缺陷、新版验证修复 | 通过 |
| 无头浏览器线上实测 | 服务端真造数 → 页面删除 → 连点刷新 → 整页刷新 | 7/7 通过 |
| 真实浏览器 + 登录态实测 | 完整用户操作路径 | 发现导航缺陷 → 修复后 7 个导航全部正常 |
| **v1.1 单元测试** | 13 工具行为 / 时间校验 / 删除硬拦截 / 撤销快照 / 双协议解析 / 会话持久化 / 六模块回归 | **44/44 通过** |
| **v1.1 集成测试** | 智能体全链路 + 12 条安全红线 + XSS 防护（自包含 headless Chrome） | **73/73 通过** |
| **v1.1 UI 巡检** | 7 视图渲染 / 双模式 / 移动端响应式 / 主题色 / JS 异常 | **19/19 通过** |

### v1.1 开发过程的测试经验

1. **IIFE 内变量单测取不到** —— 所有逻辑包在 `'use strict'` 的 IIFE 里，`vm.runInContext` 无法直接访问内部函数。解法：在 IIFE 内挂 `window.__LWB` 调试句柄，暴露 `pure`（纯函数集）与 `agent`（智能体引擎），测试统一走句柄。
2. **测试断言错误 vs 实现缺陷要分清** —— 首轮 12 项 RED 中有 4 项实际是**测试写错了**（如期望 `8:5` 被容忍为合法时间），修正断言而非改实现；剩下 8 项才是真实缺陷。
3. **"应用行为正确、测试假设错误"要敢于改测试** —— 「写操作已纳入同步快照」失败经查证是本地模式（`pullDone=false`）下 `save()` 只落 localStorage 即 return，应用行为正确；改用断言本地持久化并通过。
4. **headless Chrome 必须加 `--remote-allow-origins=*`** —— 否则 CDP 除 `Page.navigate` 外全部命令无回包（表现为超时），极难排查。
5. **测试脚本要自包含** —— Chrome 子进程在 Bash 调用结束时会被杀，导致 `ECONNREFUSED`。改为 Node 脚本内部 `spawn` 拉起 Chrome + 自带 static server，一次调用跑完全流程。

### 关键教训

1. **模拟测试有盲点**：模拟测试绕过了真实的页面渲染链路，无法发现 HTML 字面字符串类缺陷；必须用**真实浏览器 + 用户登录态**验证 UI
2. **测试有效性要自证**：修复后必须"用旧版跑同一个测试"证明测试能捕获该缺陷，否则测试可能是假阳性
3. **平台接口存在不可靠行为**：删除/更新接口存在"返回成功但实际未生效"（假成功）的情况；查询存在延迟窗口；字段读回形态与写入形态不一致 —— 客户端必须有幂等重试与保护机制兜底
4. **删除这类不可逆操作要有确认**：已加删除确认弹窗，防止误触和自动化重复点击造成批量删除

---

## 七、项目文件结构

```
README.md                               项目说明
02-原型/demo.html                       页面源码（唯一编辑入口）
06-交付/个人日程管理小助手.html         交付成品（线上即为本文件）
06-交付/交付说明.md                     交付说明
版本归档/v1.1/个人日程管理小助手.html   1.1 正式版归档
版本归档/v1.0/个人日程管理小助手.html   1.0 正式版归档
版本归档/版本索引.md                    版本索引
```

## 八、发布与维护

- **发布方式**：通过 WorkBuddy 资料库能力导入 HTML 到页面节点并发布（原节点 ID 原地更新）
- **改版流程**：修改 `02-原型/demo.html` → 同步到 `06-交付` 与 `版本归档` → 重新发布线上
- **版本查询**：页面右上角有版本号角标，与线上版本不一致说明浏览器缓存了旧页面（强制刷新即可）
- **代码归档**：GitHub 仓库 `Cathy011011/Myworkstation` 已同步定稿
- **开源工具包**：核心脚本备份于 `.workbuddy/archive-tools/`（`api-push.py` GitHub 推送 / `sync-full.py` 全量同步 / `create-tables.py` 建表 / `test-allmodules.js` 六模块回归）

---

## 九、后续可优化项

- [ ] 提交新增后自动清空输入框（当前保留输入内容）
- [ ] 顶栏状态徽章文案优化（"同步异常"易让用户紧张，可细分为"只读/登录已过期"）
- [ ] 空壳记录的自动清理机制
- [ ] 智能体对话历史跨设备同步（当前只存本机）
- [ ] 智能体流式输出（当前为一次性返回，长回答等待感较明显）
- [ ] 为智能体增加「批量操作需二次确认」（如一次要求删 5 条时逐条确认体验偏重）
