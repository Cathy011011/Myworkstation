# 「刷新云端数据」回归测试报告

- **日期**：2026-09-07
- **对象**：生活工作台 V2（https://workbuddy.link/p/8ZxrlsT0cik2jMIZ8SOM2o）
- **方式**：headless Chrome 真实操控页面 + 资料库服务端 API 模拟"另一台设备"写入
- **结论**：✅ **六模块 18/18 检查点全部通过**

---

## 一、测试方法

页面→云端的**推送**路径需要 WorkBuddy 登录态（匿名 addRecord 返回 401，见"发现"），无头浏览器无法模拟登录。因此本轮回归聚焦"刷新云端数据"按钮的**拉取同步**路径，这正是多端同步的真实场景：

1. 服务端 API 直接向六张云端表写入/修改/软删除测试记录（等价于另一台设备上的变更）；
2. headless Chrome 打开线上页面，真实点击「刷新云端数据」按钮；
3. 双重断言：页面 SDK 查询云端计数 + 页面 DOM 实际渲染内容；
4. 每步等待同步徽章回到「●已同步」再断言。

**每模块 3 个检查点**：

| 检查点 | 场景 | 断言 |
|---|---|---|
| A1 新增→刷新 | 服务端新增标记记录后点刷新 | 页面出现该记录；未删除记录数=基线+1；标记仅 1 条（不丢、不重） |
| A2 改名→刷新 | 服务端改标题后点刷新 | 页面显示新标题；旧标题零残留；条数不变 |
| A3 软删→刷新 | 服务端打「已删除」标记后点刷新 | 记录从页面消失（不复活）；未删除记录数回到基线（不误删其他数据） |

## 二、结果矩阵（全部 PASS）

| 模块 | 基线条数 | A1 新增→刷新 | A2 改名→刷新 | A3 软删→刷新 |
|---|---|---|---|---|
| 日程 | 3 | ✅ 不丢不重 | ✅ 新旧标题正确切换 | ✅ 不复活不误删 |
| 待办 | 6 | ✅ | ✅ | ✅ |
| 翻译 | 3 | ✅ | ✅ | ✅ |
| 问答 | 3 | ✅ | ✅ | ✅ |
| 日程规划 | 1 | ✅ | ✅ | ✅ |
| 信息整理 | 3 | ✅ | ✅ | ✅ |

- 全程同步徽章保持「●已同步」，无「异常」状态。
- 18 张过程截图存于 `04-测试/screenshots/regress2-*.png`。
- 原始结果数据：`07-脚本/regression-v2-result.json`。

## 三、发现的问题

### 【中】分享页写入接口对未登录访客返回 401
- `POST /space/api/page/share/database/addRecord`、`updateRecord` 在**未登录浏览器**上返回 401（查询接口匿名可用）；
- 401 后宿主框架会把 iframe 重定向到 codebuddy 登录页，应用不可见；
- **影响**：未登录 WorkBuddy 的设备打开链接时，新增/删除只留在本地并显示"未同步"徽章（数据不丢，但不上云）。多端同步的前提是各设备浏览器处于登录态。
- **建议**：确认常用设备均登录；产品层面可考虑分享页写入的匿名授权或给出明确的"请登录"引导，而不是静默 401 + 跳登录。

### 【低】缺失日期字段时分组标题渲染 "NAN月NAN日"
- 服务端只写「日期键」不写「日期」（date 类型）字段时，日程按日期分组标题显示 `NAN月NAN日 星期日`（见截图 regress2-schedule-a1-add.png 顶部）。
- **建议**：`cnDate`/`fromRecord` 对无效日期兜底（如回退到日期键或显示"—"）。页面自身新增的记录两个日期字段都会写，正常使用不受影响。

### 【信息】控制台偶发网络抖动
- 测试期间捕获少量 `ERR_CONNECTION_REFUSED / ERR_CONNECTION_TIMED_OUT`（多为统计脚本与偶发抖动），未影响同步结果。

## 四、覆盖范围说明

- ✅ 本轮覆盖：云端→页面拉取全路径、六表、增/改/软删三类变更、计数一致性、DOM 一致性。
- ⚠️ 未覆盖：页面→云端**推送**路径（需登录态）。该路径此前已通过多轮专项修复验证：软删除防幽灵复活、删除幂等（delFail）、空壳推送修复、pullAll 防覆盖等，且本轮 A1–A3 的服务端写入与页面拉取联动间接验证了同一批数据通路。
- 测试数据已清理：所有测试记录均以「已删除」标记结束（与真实删除路径一致），页面与云端查询均不可见。

## 五、产物清单

| 文件 | 说明 |
|---|---|
| `07-脚本/regression-v2.js` | 可重复执行的回归脚本（服务端写入 + 浏览器验证） |
| `07-脚本/regression-v2-result.json` | 本轮原始结果 |
| `04-测试/screenshots/regress2-*.png` | 18 张分步截图 |
