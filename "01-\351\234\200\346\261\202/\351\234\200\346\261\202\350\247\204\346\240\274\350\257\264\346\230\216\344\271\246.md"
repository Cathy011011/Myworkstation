# 需求规格说明书 · 日常集（新版）

> 母版：《日常集 · 生活有迹可循》 https://workbuddy.link/p/JlDtfA6n49ote46Jv3lHOn
> 本版继承母版的**视觉与容错架构**，业务内容全部替换为六个新模块。

## 一、目标与边界

| 项 | 内容 |
|---|---|
| 要继承的 | 手账视觉体系、多端自适应、先渲染界面再拉数据的加载顺序、读写失败的提示与重试机制 |
| 要替换的 | 全部业务模块与数据表。原六个模块（记账理财·习惯健康·减脂健身·日程统筹·待买清单·书影音）及其六张表**不迁移、不保留** |
| 要新建的 | 六张资料库数据表 + 一个在线页面，双向同步 |
| 不动原页面 | 母版作为只读参考存放于 `03-源站参考/`，任何操作不修改它 |

## 二、模块定义

### 1. 今日总览（默认首页）
- 首屏 hero 卡：今天的日程数、未完成待办数、待 AI 处理条目数
- 三个指标卡：本周规划进度、语料与问答总量、已整理信息数
- 今日时间轴 + 待办清单 + 最近整理的信息
- **不建第七张表**，并发查六张表聚合

### 2. 日程
定义：**某天几点要做的具体事项**。按日期分组的时间轴视图，支持地点、类型、起止时间、提醒。
新增表单：标题 / 日期 / 开始 / 结束 / 类型 / 地点。

### 3. 待办
定义：**没有固定时间点的任务**。四象限优先级 + 领域分类 + 截止日期 + 所属周。
交互：勾选完成、按优先级与完成状态筛选。

### 4. 翻译
定义：**实时翻译 + 双语语料沉淀**。输入原文 → 实时得到译文 → 落库形成可检索语料。
字段承载：源语言 / 目标语言 / 模型 / 状态 / 处理方式 / 错误信息 / 收藏 / 标签。
演示阶段用本地模拟结果，正式版接用户自己的模型。

### 5. 问答
定义：**实时问答 + 知识沉淀**。提问 → 得到回答 → 落库成可复用知识卡片。
字段承载：评分、标签、归档状态，支持按标签检索与归档。

### 6. 日程规划
定义：**周期性的阶段安排**，与「日程」形成两层结构——这里管的是一整周或一整个月，不具体到几点。
卡片包含：周期类型、起止日期、进度条、状态、关键结果、领域。支持推进进度。

### 7. 信息整理
定义：**摘录 + 结构化笔记**。四件内容分开存：标题、来源、原文摘录、自己的笔记。
标签检索、关键词搜索、归档。

## 三、视觉契约（取自母版实测值，不得随意改动）

```
--paper #f5f0e8   页面底色（米黄纸感）
--card  #fffdfa   卡片（暖白）
--ink   #29251f   主文字
--muted #746d63   次要文字
--line  #ded5c8   描边
--plum  #4d3045 / plum-soft  #e8dfe5   梅紫
--terra #b65f42 / terra-soft #f3dfd6   陶土橘
--sage  #627a67 / sage-soft  #dfe8df   鼠尾草绿
--sand  #a57c45 / sand-soft  #eee2ce   沙棕
--red   #b44d43   --green #477358
--shadow 0 16px 40px rgba(63,48,36,.07)
--radius 22px     --sidebar 248px（≤860px 时归零）
sidebar 底 #ede6dc，右边框 1px rgba(104,87,68,.12)
字体 Inter, PingFang SC, Microsoft YaHei, sans-serif
按钮 高 42px 圆角 12px 13px/700；输入框 高 44px 圆角 11px 边框 #ddd4c8
panel 内边距 22px；栅格 gap 16px
提示条 底 #f5ded2 边框 #dba98f 圆角 15px
断点 1280 / 1180 / 860 / 560
```

装饰手法：不使用 radial-gradient 噪点纹理，不滥用虚线。图标统一 24×24 stroke 线性风格，走 SVG sprite。
≤860px：侧栏收起，改用底部导航，顶栏压缩，栅格转单列。

## 四、同步契约

- 页面所有读写直接走 `window.__SMART_PAGE__.database`，**不以本地为准**
- 新增、修改、删除即时写回线上表
- 换设备、换浏览器打开看到的必须是同一份数据
- 六个 `databaseId` 硬编码写入 HTML，并用 `getSchema()` 动态读字段与选项，避免字段名硬耦合

## 五、容错契约（继承母版并强化）

| 场景 | 要求 |
|---|---|
| 页面加载 | 先渲染界面骨架，再拉数据；不得白屏等待 |
| 读取失败 | 明确提示，提供重试入口，不清空已有内容 |
| 写入失败 | 明确提示哪一条没写进去，内容保留在输入框里，提供重试 |
| 单模块失败 | 只降级这一个模块的卡片，其它模块照常工作 |
| 本地兜底 | 仅保留「写入失败重试队列」，不做全量本地镜像，避免双端冲突 |

## 六、AI 接入方案

页面无法调用平台内置模型（运行时注入对象只有 `database`），因此：

1. **主方案**：页面直连用户自己的模型（DeepSeek / 通义 / Kimi / OpenAI 兼容接口）
   - 密钥存 `localStorage`，不在 HTML 里留明文，也不写进资料库
   - 页面提供设置面板与「测试连接」
2. **前置验证**：动手前先上传一个探针页，实测沙盒是否放行跨域请求。通走主方案，不通即刻降级
3. **降级方案**：页面把待处理条目写进数据表（状态=待处理、处理方式=代理回写），由 WorkBuddy 在对话侧处理后 `batch_update` 回写，页面轮询显示结果
4. 两种方案共用同一套状态字段，切换不需要改表结构

## 七、交付形态

- 资料库协作态在线页面（`/space/d/{nodeId}`），**不自动发布**
- 六张独立数据表挂在页面下
- 本地保留：需求文档、字段字典、原型、母版、建表 schema、部署脚本

## 八、验收标准

- [ ] 七个视图（含首页）在桌面与手机都能正常切换、无横向溢出
- [ ] 六张表各自的增删改即时生效，刷新后数据仍在
- [ ] 换一个浏览器打开同一链接，看到的是同一份数据
- [ ] 断网时页面仍渲染出界面，并给出可读的错误提示与重试按钮
- [ ] 翻译/问答能产出结果并进入列表，带正确的状态标记
- [ ] 页面内无任何第三方域名的图片外链
