# 缺陷清单

> 登记规则见 `04-测试/测试计划.md`。级别：阻断 / 严重 / 一般 / 提示。

## 已关闭

### DEF-01 · 问答视图列表恒为空

| 字段 | 内容 |
|---|---|
| 发现阶段 | 阶段一 原型评审（自动化测试 T7） |
| 关联用例 | T7（以及 T2 的异常数据） |
| 现象 | 打开问答模块，只显示空状态提示；但顶部筛选标签能正常列出「沟通 / 设计 / 教学」，说明数据其实存在 |
| 复现步骤 | 启动 Demo → 点击侧栏「问答」→ 观察知识库区域 |
| 实际结果 | 无卡片，显示空状态 |
| 期望结果 | 显示种子数据的 3 条问答卡片 |
| 根因 | `state` 初始化时遗漏 `qaFilter` 字段，其值为 `undefined`。过滤逻辑中 `state.qaFilter !== '全部'` 成立，于是每条数据都去匹配 `tags.indexOf(undefined)`，返回 -1，全部被过滤 |
| 级别 | 严重（整个模块不可用） |
| 修复 | 在 `state` 初始化中补 `qaFilter:'全部'` |
| 回归 | T7 复测通过；问答视图内容长度由 1563 升至 3061，卡片正常渲染 |
| 状态 | 已关闭（2026-09-07） |

---

## 打开中

（暂无）
