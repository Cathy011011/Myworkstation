# 测试报告 · 生活工作台 V2 原型

| 项 | 内容 |
|---|---|
| 测试时间 | 2026-09-07 15:48 |
| 被测对象 | `02-原型/demo.html`（本地演示版，七视图六模块） |
| 执行方式 | **真实浏览器自动化**：本机 Chrome 152 headless + CDP 驱动，真实点击、真实填写、真实发起网络请求 |
| 测试脚本 | `07-脚本/browser-test.js`（Node 原生 WebSocket 直连 CDP，零第三方依赖） |
| 服务环境 | 本地 `http.server`（127.0.0.1），避免 `file://` 的 null origin 干扰跨域判定 |
| 结果 | **通过 11 / 11**，总耗时 41.2s |
| 原始数据 | `04-测试/last-run.json` |

---

## 一、逐项结果

| 用例 | 项目 | 结果 | 实测数据 |
|---|---|---|---|
| T1 | 首屏渲染 | 通过 | 标题「今日总览」，导航 7 项，hero 卡存在，DOM 54KB |
| T2 | 七视图切换 | 通过 | 七个标题各不相同，内容长度 3061–4587 |
| T3 | 日程新增 | 通过 | 列表 5→6，新条目出现在页面 |
| T4 | 待办勾选 | 通过 | 勾选状态 false→true |
| T5 | 待办筛选 | 通过 | 筛选芯片 6 个，全量 5 条 → 未完成 3 条 |
| **T6** | **翻译 · LLM 真实应答** | **通过** | 见下方专项 |
| **T7** | **问答 · LLM 真实应答** | **通过** | 见下方专项 |
| T8 | 规划进度推进 | 通过 | 进度 65% → 75% |
| T9 | 信息整理新增 | 通过 | 卡片 3→4，摘录与笔记分别入库 |
| T10 | 刷新后持久化 | 通过 | reload 后新增数据仍在，模型配置保留 |
| T12 | 移动端 375 布局 | 通过 | 底部导航 flex、侧栏 none、横向溢出 0px |

---

## 二、LLM 应答专项（重点）

配置来源与 WorkBuddy 共用同一中转站（OpenAI 兼容协议），密钥仅临时注入浏览器 localStorage 用于测试，**未写入任何产物**。

### T6 翻译

- 输入：`请把第三节的课程大纲发我一份`（中文 → 英文）
- 等待：**11.3 秒**
- 输出：`Please send me a copy of the course outline for Section 3.`
- 入库校验：语料库 3 → 4 条，记录显示「中文 → 英文 / 已完成 / 页面直连」

### T7 问答

- 输入：`怎么和家长沟通孩子作业拖延？给三个具体做法`
- 等待：**23.4 秒**
- 输出：结构化三点回答（用事实代替评价 / 共同分析原因 / 给出可操作建议并约定反馈）
- 入库校验：知识库 3 → 4 条，卡片含完整回答

### 稳定性观察

三轮实测耗时分别为 **32.4s / 40s（未返回）/ 23.4s**，波动明显。判断是中转站侧响应不稳定，非页面问题——页面在任何一次失败/超时的情况下都保留了用户输入并给出可重试提示，未出现静默失败或数据丢失。

> **重要边界**：本次是在 `http://127.0.0.1` 本地域下直连成功。这证明**服务端 CORS 放行**（已单独验证 `Access-Control-Allow-Origin: *`），但**不等于资料库线上 iframe 沙盒一定放行**——沙盒可能有独立的 CSP / 网络策略。这一项仍需上传探针页单独验证。

---

## 三、发现并修复的缺陷

| 编号 | 现象 | 根因 | 级别 | 状态 |
|---|---|---|---|---|
| DEF-01 | 问答视图列表恒为空，只显示空状态，但筛选标签能正常列出 | `state` 初始化遗漏 `qaFilter`，值为 `undefined`，导致每条数据都走 `tags.indexOf(undefined)` 分支被过滤掉 | 严重 | **已修复**（补 `qaFilter:'全部'`），修复后该视图内容长度由 1563 升至 3061 |

这个缺陷是靠 T2 的异常数据（问答视图内容长度明显低于其他屏）暴露出来的，说明逐屏比对内容规模这个断言是有效的。

---

## 四、测试脚本自身的两处修正（非产品缺陷）

记录在此，避免后续复现时误判：

1. **元素引用失效**：点击导航后页面整屏重绘，此前持有的 DOM 节点变成游离节点，读到的一直是旧文本。修正为每次按需重新查询。
2. **误判"调用失败"**：用 `body.innerHTML.includes('调用失败')` 判定失败，而 `<script>` 源码里就含这个字符串，恒为真。修正为只检查输出区元素自身的文本。

---

## 五、本次未覆盖

以下项依赖线上数据表，原型阶段无法验证：

- 跨设备 / 跨浏览器数据一致性
- 读取失败与写入失败的提示与重试
- 单模块降级不影响其他模块
- 超过 50 条时的翻页（queryAll）
- 线上 iframe 沙盒内的跨域连通性

---

## 六、结论

原型阶段 **11 项全部通过**，翻译与问答均已跑通真实模型调用，不是模拟结果。发现 1 个严重缺陷（问答列表为空）已修复并复测通过。

下一步建议：上传探针页验证线上沙盒跨域 → 建六张线上数据表 → 改造页面上传，然后按 `04-测试/测试计划.md` 的阶段三至九执行完整测试。

## 附：截图

- `04-测试/screenshots/desktop-overview.png` —— 桌面端今日总览
- `04-测试/screenshots/desktop-translate.png` —— 桌面端翻译（含真实译文）
- `04-测试/screenshots/mobile-overview.png` —— 移动端 375×812 今日总览
